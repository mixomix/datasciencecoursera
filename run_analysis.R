# this script requires you to be in the working directory of the data 
# - that is the one where README.txt of the dataset is

name_list <- read.table("train/x_train.txt")

train <- read.table("train/x_train.txt")


names(train) <- 